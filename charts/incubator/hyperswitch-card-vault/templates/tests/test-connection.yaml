apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hyperswitch-card-vault.fullname" . }}-test-connection"
  labels:
    {{- include "hyperswitch-card-vault.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hyperswitch-card-vault.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
