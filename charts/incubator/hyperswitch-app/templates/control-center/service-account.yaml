{{- if .Values.services.controlCenter.enabled }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: hyperswitch-control-center-role
  namespace: {{ include "controlCenter.namespace" . }}
{{- end }}
