{{- if and .Values.services.router.enabled .Values.server.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "hyperswitch.router.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
  {{- with (coalesce .Values.server.serviceAccount.annotations .Values.global.serviceAccountAnnotations .Values.global.annotations) }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
